# DMS Completed Tasks

## cli

- [cli-refresh-type-refactor.md](./tasks/completed/cli-refresh-type-refactor.md) - DMS CLI refresh for the modern type scheme + per-app tables + dmsEnv ownership: new `cli/src/utils/types.js`; every `byId` path app-namespaced; `dataset list` walks `pattern.dmsEnvId → dmsEnv.sources`; `dataset dump`/`query` go through the hydrating `options` Falcor route for split tables; `cms-section` → `component`; docs (TYPES, CLAUDE) rewritten; seed + 21/21 integration tests on the modern shape; live-verified against `asm+nhomb` (2026-05-01)
- [dms-cli.md](./tasks/completed/dms-cli.md) - DMS CLI tool: Falcor HTTP client, raw CRUD, site/pattern/page/section/dataset commands, tree formatter, stdin support, 21 integration tests, docs (2026-02-09)

## api

- [datawrapper-api-loading.md](./tasks/completed/datawrapper-api-loading.md)
- [falcor-loader-parallel-requests.md](./tasks/completed/falcor-loader-parallel-requests.md) - Falcor loader parallel requests: combined length + data fetches into parallel requests for faster page loads (2026-03-16) - DataWrapper API-layer loading: moved data fetching into React Router 7 loader via `preload` hook pattern; slug-based page matching, dataRequest enrichment to match component's runtime builder, page filter resolution + injection; eliminates post-mount data fetch waterfall for Spreadsheet/Card/Graph sections (2026-02-25)
- [datawrapper-skip-fetch-when-cached.md](./tasks/completed/datawrapper-skip-fetch-when-cached.md) - DataWrapper skip fetch when cached: Graph + other non-paginated components now honor the "Always Fetch Data" toggle. Pagination's auto-set of `readyToLoad=true` no longer leaks into persisted `element-data`, so visits with the toggle OFF serve straight from cached state with no API call (2026-04-24)
- [datawrapper-rearchitecture.md](./tasks/completed/datawrapper-rearchitecture.md) - DataWrapper & data sources re-architecture: shipped `buildUdaConfig` (UDA config construction extracted), `useDataLoader` (data loading + caching extracted from section), `outputSourceInfo` (each data-driven component publishes a sourceInfo describing its output for downstream chainability — wired into spreadsheet/card/graph/filters), and developer docs (`page-pattern-guide.md`). Phases 3 (clean section↔dataWrapper interface), 5 (page-level data sources + state ownership restructure), and 5B (clean data schema) deferred — the load-bearing pieces are in production; remaining phases would each be their own task if revisited (2026-04-24)
- [datawrapper-time-filters.md](./tasks/completed/datawrapper-time-filters.md) - Time filters in dataWrapper: new `op: 'time'` filter leaf shipped end-to-end across six phases. P1 server Postgres predicates (`time-filter.js` + UDA tree-walk wiring; 12 new test sections, 58 UDA tests pass). P2 client primitive — TimePicker subtree, `useNowTick` boundary-aware refetch (no polling), URL token roundtrip wired through `applyPageFilters`. P3 compositions — DOW + time-of-day rows, multi-range OR via `|`, per-axis chips (52 URL roundtrip assertions); midnight-wrap windows forbidden in v1 with inline UX warning. P4 `instant` + `compareEnd` — InstantRow with end-column select, `end:colname` URL axis token (25 assertions). P5 author/viewer axis exposure — `value.exposedAxes`, `mergeUrlOntoExposedAxes` for selective URL merge in `buildUdaConfig.applyPageFilters`, picker `mode='author'|'viewer'` (21 assertions). P6 ClickHouse predicate emitters — `buildTimeFilterCH` covers every axis (toTimezone / toStartOf* / toDayOfWeek %7 normalization / formatDateTime HH:MM); CH dispatch wiring deferred to broader CH-tree-based-filter migration. Cleanup: TimePicker subtree pulled into `timePicker.theme.js` per the new theming guidance — ~40 named keys, registered in `patterns/page/defaultTheme.js`, shared via `useTimePickerTheme` hook (2026-05-07)
- [wcdb-schedule-now-playing-card.md](./tasks/completed/wcdb-schedule-now-playing-card.md) - WCDB schedule + now-airing card: first real consumer of the `op:'time'` `instant`+`compareEnd` primitive. Shipped Option A (calc-column bridge) — hidden `start_at`/`end_at` calc columns project the recurring `(show_day_start, show_start, show_day_end, show_end)` slot onto absolute `timestamptz`; Card section bound to the WCDB Schedule times view + `wcdb_djs` join with time-filter `{kind:'instant', at:'now'}` + `compareEnd:'end_at'`, page size 1, `useNowTick` handles boundary refetch. Surfaced and fixed four real client/server bugs: `compareEndAccessor` field on time-filter for calc-col end columns, `mapFilterGroupCols` alias resolution gap in `buildUdaConfig.js`, `aliasCalcSql` regex prefixing `data->>` with the correct table alias inside calc expressions under joins, and `getData.js:313` join post-processor mishandling calc cols whose `type` is the return type (`text`) rather than literally `'calculated'` (now uses `isCalculatedCol`). Bonus framework wins: `portrait_banner` column type with theme-driven `bannerHeights` map (fill/full/tall/medium/small), per-column-type `cardControls` registry that flattens declared controls into Card's `inHeader` (Card.config `controls` is now a function so the resolution sees theme-registered types and the live theme), font-style dropdown sourced from `theme.textSettings` so `h1..h6`/`body`/`caption` show up alongside `text*`, `cellSpan`/`cellRowSpan` now override `cardHints.spanFullColumns`/`spanFullRows`. Recipe captured in `skills/now-airing-card.md`; broader how-to in `skills/creating-page-section-components.md` (2026-05-08)

## dms-manager

- [centralize-format-initialization.md](./tasks/completed/centralize-format-initialization.md) - Centralized `updateRegisteredFormats`/`updateAttributes` in `_utils.jsx`, added `initializePatternFormat` helper; removed ~100 lines of duplicated code from admin/page/forms/datasets patterns (2026-02-05)

## dms-server

- [fix-csv-analyze-large-file-string-limit.md](./tasks/completed/fix-csv-analyze-large-file-string-limit.md) - csv processor `analyze()` read the whole file as one string just to get the header row, so any CSV >~512 MB (Node max string length) failed upload with "Cannot create a string longer than 0x1fffffe8 characters"; now streams the first line (bounded `readFirstLine`). Regression test (sparse 600 MB file) + live-verified with the 2 GB TRANSEARCH flow CSVs. **Hosted dmsserver.availabs.org needs a redeploy to pick this up.** (2026-06-12)
- [port-pattern-duplicate-route.md](./tasks/completed/port-pattern-duplicate-route.md) - Ported the pattern **Duplicate** route to dms-server (was avail-falcor-only → button 404'd silently → empty duplicates). New `POST /dama-admin/dms/:appType/duplicate` + `dama/upload/dms-duplicate.js`: new-type-model deep clone of `{instance}|page` + `{instance}|component` rows under the new instance, remapping `draft_sections`/`sections` refs + section `parent` + `data.id` mirrors (history dropped; pattern row still client-created via `addNewValue`), built on the controller (split-mode safe). Client (`patternList.jsx`/`editSite.jsx`) now checks `res.ok` and surfaces failures. Verified: freightatlas2 → 7 pages/195 components, refs remapped; test rows cleaned. UI end-to-end pending user confirm (2026-06-10).
- [gis-publish-worker-async-progress.md](./tasks/completed/gis-publish-worker-async-progress.md) - GIS publish worker async progress + tile-URL fallback: ported `dama/upload/workers/gis-publish.js` from blocking `execFileSync` to async `spawn`. Worker event loop now stays alive during ogr2ogr — streams stderr into `gis-dataset:ogr2ogr_progress` events, parses `-progress` percent markers to climb `tasks.progress` from 0.10 → 0.70, emits a 15s `gis-dataset:ogr2ogr_heartbeat` while the child is alive, and surfaces non-zero exit / signal as a rejected promise (cause of the two `npmrds2` tasks 6771/6773 that sat at `progress=0.10` indefinitely). Bundled in the same file: `DAMA_SERVER_URL` → `DMS_PUBLIC_URL` env-var rename (with deprecation warning when only the legacy var is present) and hardcoded fallback to `https://dmsserver.availabs.org` when neither is set, with a `gis-dataset:public_url_missing` task event (`fallback_url` payload) + console warning so non-AVAIL deploys can discover and override the default. Unit-verified: 12 assertions across the three resolvePublicUrl branches + 8 assertions on runOgr2OgrAsync (success / non-zero exit / SIGKILL); live mid-load testing deferred to real PostgreSQL deploy (2026-05-26)
- [uda-source-lookup-ambiguity.md](./tasks/completed/uda-source-lookup-ambiguity.md) - UDA `getEssentials` source-id lookup deterministic: `ORDER BY id DESC LIMIT 1` in `routes/uda/utils.js` silently routed to the wrong source when two rows shared `(app, {instance}:source)`; resolution now goes through `view_id ∈ source.data.views` so the matching owner row is picked unambiguously. Removes the load-bearing role of the 2026-04-24 "Songs"-incident band-aids (practice_recordings rename, 409 guard in file-upload route) (2026-05-07)
- [fix-createData-drops-data.md](./tasks/completed/fix-createData-drops-data.md) - Fixed createData ignoring data argument, causing sections to have empty data (2026-01-28)
- [fix-sqlite-pages-missing-index.md](./tasks/completed/fix-sqlite-pages-missing-index.md) - Fixed searchOne returning null for root page: `||` dropping falsy `0` + SQLite `->>` type mismatch (2026-01-28)
- [dms-server-auth.md](./tasks/completed/dms-server-auth.md) - Implemented full auth system: JWT middleware, 45 endpoints (auth/user/group/project/message/preferences), cross-DB queries, authority checks, created_by/updated_by pipeline, 103 integration tests (2026-02-09)
- [dms-server-postgres-tests.md](./tasks/completed/dms-server-postgres-tests.md) - PostgreSQL test support: Docker lifecycle helper, parameterized test-graph/test-workflow/test-auth for dual DB, npm scripts (test:pg, test:all), fixed COUNT bigint + boolean cross-DB bugs (2026-02-09)
- [uda-routes.md](./tasks/completed/uda-routes.md) - UDA Falcor routes: unified query interface for DMS (JSON data_items) and DAMA (sources/views/tables) databases, PostgreSQL only (2026-02-08)
- [auth-db-init-race.md](./tasks/completed/auth-db-init-race.md) - Fixed auth DB init race condition: `getDb()` returns before async init completes causing "no such table: users"; added `awaitReady()`, multi-role config support (2026-02-08)
- [fix-cleanup-orphaned-pages.md](./tasks/completed/fix-cleanup-orphaned-pages.md) - Fix orphaned pages detection: disabled false-positive-prone pages detector from --delete mode, added page_edits orphan detection, skipData memory optimization (2026-03-11)
- [dms-dead-row-cleanup.md](./tasks/completed/dms-dead-row-cleanup.md) - Dead row cleanup CLI: analyzes DMS database for 5 orphan types (patterns, pages, sections, sources, views), PostgreSQL-optimized SQL path, optional --delete mode, 40 integration tests (2026-02-13)
- [dms-db-copy.md](./tasks/completed/dms-db-copy.md) - Database copy CLI: cross-DB copy (PG↔SQLite), ::TEXT cast optimization, unnest() bulk PG inserts, split table discovery, sequence sync, --batch-size flag, 61 integration tests (2026-02-13)
- [dms-server-file-upload.md](./tasks/completed/dms-server-file-upload.md) - File upload routes: CSV/Excel upload, publish, and validate as standalone synchronous endpoints in dms-server (2026-02-23)
- [split-table-naming.md](./tasks/completed/split-table-naming.md) - Split table naming: `data_items__s{sourceId}_v{viewId}_{docType}` format, `parseType()` helper, async source_id lookup with cache, graceful fallback, migration script (2026-02-23)
- [dms-table-splitting.md](./tasks/completed/dms-table-splitting.md)
- [per-app-pg-schemas.md](./tasks/completed/per-app-pg-schemas.md) - Per-app PostgreSQL schemas: `dms_{appname}` schemas instead of table name prefixes in per-app split mode; `resolveSchema()`, `ensureSchema()`, updated `resolveTable()`/`getSequenceName()`, removed stale guards in controller/UDA/sync (2026-03-16) - Table splitting Tier 2: app-namespaced byId/edit routes (dual-route compat), client API changes (api/, CLI, patterns — ~25 call sites), 34 Tier 2 tests, migrate-to-per-app.js script, API docs (2026-02-23)
- [extract-lexical-images.md](./tasks/completed/extract-lexical-images.md) - Extract embedded images: scans data_items for base64 data URIs in Lexical nodes and map component `img` fields, extracts to files, replaces with URL paths; PG cursor with SQL-side filtering, per-app output dirs, SHA-256 deduplication (2026-03-16)
- [search-tags-performance.md](./tasks/completed/search-tags-performance.md) - Search tags query optimization: replaced CTE+json_each+CAST join with direct section query, added partial expression index `(app, type, json_extract(data, '$.tags'))`, server-side 60s TTL cache. Production result: 264s → 6ms (44,000x speedup) (2026-03-03)
- [sqlite-compat-fixes.md](./tasks/completed/sqlite-compat-fixes.md) - SQLite compatibility: ID type normalization (String coercion in all $ref paths and byId responses), PG→SQLite SQL translation (`array_agg`→`group_concat`/`json_group_array`, `to_jsonb(array_remove(array[...]))`→`json_array(...)`, `::text`→`typeCast`), 38 tests (2026-03-04)
- [per-config-split-mode.md](./tasks/completed/per-config-split-mode.md) - Per-config split mode: moved `DMS_SPLIT_MODE` from server-wide env var to per-database-config `splitMode` field, with env var fallback for backward compatibility (2026-03-17)
- [test-suite-per-app-mode.md](./tasks/completed/test-suite-per-app-mode.md) - Test suite per-app mode: migrated all test code from legacy `byId` route to app-namespaced route, set all test configs to `splitMode: "per-app"`, verified on SQLite + PostgreSQL (2026-03-17)
- [fix-auth-test-pg-socket-hangup.md](./tasks/completed/fix-auth-test-pg-socket-hangup.md) - Fixed auth test #14 PG ECONNRESET: `req.on('close')` in falcor-express fired prematurely in Node.js v15+ (request body consumed ≠ client disconnect); changed to `res.on('close')` with `!res.writableFinished` guard (2026-03-17)
- [uda-array-contains-filter.md](./tasks/completed/uda-array-contains-filter.md) - UDA array_contains + array_not_contains filter ops: server-side `buildLeafSQL` with cross-DB SQL (PG `jsonb_array_elements_text` / SQLite `json_each`), threaded `dbType` through filter chain, client `mapFilterGroupCols` converts multiselect ops, removed ~235 lines of async multiselect resolution from utils.jsx (`resolveMultiselectInFilterGroups`, legacy flat filter resolution, old async `mapFilterGroupCols`, duplicate extract helpers), 12 new tests (51 total UDA), all pass on SQLite + PostgreSQL (2026-03-27)
- [uda-clickhouse-support.md](./tasks/completed/uda-clickhouse-support.md) - UDA ClickHouse support for DAMA pgEnv: new `ClickHouseAdapter` + `getChDb(pgEnv)` factory, `query_sets/{index,postgres,clickhouse,helpers}.js` dispatch layer, `getEssentials` detects `clickhouse.<schema>` prefix in `data_manager.views.table_schema` and swaps adapter; source/view metadata stays in PG; live smoke test + cross-DB meta lookup verified against npmrds2 (2026-04-23)
- [uda-getsitepatterns-instance-matching.md](./tasks/completed/uda-getsitepatterns-instance-matching.md) - Fix UDA `getSitePatterns` / `getSiteSources` for new type scheme: replaced substring `type LIKE '%docType%'` match with exact instance-segment match (`type LIKE '%|' || $instance || ':pattern'`), dropped `data->>'doc_type'` filter and unused `pattern_doc_types` param, rewrote two broken test fixtures to new-format types. All 51 UDA tests pass on SQLite + PostgreSQL (2026-04-23)
- [cleanup-protect-dmsenv-sources.md](./tasks/completed/cleanup-protect-dmsenv-sources.md) - CLOSED as unnecessary: the type-system refactor moved source ownership into the type column (`{dmsEnv}|{name}:source`), making the original pattern-doc_type-based orphan detection model obsolete; cleanup-db.js needs a fresh redesign around dmsEnvs rather than this incremental patch (2026-04-23)
- [dama-now-playing-datatype.md](./tasks/completed/dama-now-playing-datatype.md) - now_playing dataType plugin: ACRCloud webhook receiver shipped as a DMS plugin at `data-types/now_playing/` — pre-provision Create page collects stream + ACR project/stream IDs and (optionally) a Console-bearer-token backfill window; provisioned source/view writes `metadata.columns` (curated 18-column subset of the wide `JSONB raw` row); webhook handler validates `?key=<secret>` against `data_manager.sources.statistics.webhook_secret`, normalizes via shared `normalize.js`, inserts via `schema.js` with `ON CONFLICT DO NOTHING` against a partial unique index on `(acrid, timestamp_utc) WHERE kind='matched'` for idempotent re-runs; backfill worker pages ACR Console results-by-date with deliberate "all-time" expansion (`2010-01-01 → today UTC`) since the v2 API silently filters to today; iTunes Search + MusicBrainz/Cover Art Archive cover enrichment fills `album_cover` (ACR doesn't return it); Webhook source-view page surfaces the callback URL with copy + rotate-secret + health + on-demand backfill so users have a stable place to come back to. Existing `Card` page-section bound to a stream's view renders the latest matched track on a DMS page. Live-verified against ACR project 16608 stream `s-Z0XwkcHp` (2026-05-01)
- [datatypes-plugin-infrastructure.md](./tasks/completed/datatypes-plugin-infrastructure.md) - Datatypes plugin infrastructure: `dms-template/data-types/` + `dms-template/server/register-datatypes.js` bootstrap module; submodule `dms-server/src/index.js` reads `DMS_EXTRA_DATATYPES` and calls the consumer's `registerExtra({ registerDatatype })` after pmtiles; `package.json {type:commonjs}` shims override the ESM root for the plugin subtree; throwaway `_hello-world` plugin verifies the round-trip end-to-end (POST `/dama-admin/<pgEnv>/hello-world/ping` → events stream `INITIAL → FINAL` → task `done`). Prerequisite for `dama-nfip-claims-migration` and `dama-map21-migration` (2026-04-26)
- [dama-csv-analyzer.md](./tasks/completed/dama-csv-analyzer.md) - DAMA CSV analyzer: ported legacy per-value `analyzeSchema.js` as the default CSV/TSV type detector (zero-padding + FIPS/GEOID heuristics, 10K-row state machine, BIGINT overflow → TEXT, scientific notation → NUMERIC, null/nonnull counts + per-type sample collection); fixed `generateTableDescriptor` rename bug by pairing `fieldsMetadata` ↔ `schemaAnalysis` by index instead of name (regression for view 3384's 19 integer-as-TEXT columns); kept `DAMA_CSV_ANALYZER=ogrinfo` fallback path; 22 new csv-analyzer tests + 12 upload-pipeline tests still passing; production-verified end-to-end; `docs/upload.md` got a new "Analysis Pipeline" section + env var entry (2026-04-23)
- [dama-server-port.md](./tasks/completed/dama-server-port.md) - DAMA server port to dms-server: ported task queue (host isolation + `FOR UPDATE SKIP LOCKED` + forked workers + event tracking), GIS/CSV upload pipeline (GDAL via gdal-async + ogrinfo), storage abstraction (local + S3), datatype plugin system, dynamic MVT tile serving, multi-pgEnv routing with auto-discovery, in-place migration script (4,806 tasks + 7,844 events on npmrds2), Docker image with GDAL/Sharp/Tippecanoe; client migrated to UDA-based task pages, ExternalVersionControls, GIS Create wizard, mapeditor, datasources; deleted legacy `dama-info`/`dama_falcor` plumbing; downstream tasks split out as `dama-csv-analyzer`, `dms-task-system`, `remove-events-query-shim` (2026-04-23)
- [dms-task-system.md](./tasks/completed/dms-task-system.md) - DMS task system independent of DAMA: new `dms.tasks` + `dms.task_events` schema (PG + SQLite) wired into dms-role init with boot-time `recoverStalledTasks`; `src/dms/tasks/{index.js,worker-runner.js}` mirrors DAMA surface (queueTask/claimTaskById/dispatchEvent/runInlineTask/recoverStalledTasks/startPolling) with `app` column delta and DMS-db single target; UDA tasks controller dispatches by `env.includes('+')` so DMS env hits the new module while pgEnv still goes to DAMA — Falcor path unchanged; `getTaskById` no longer app-scoped; internal_table publish wrapped in `runInlineTask` emitting `publish:input/columns/source-resolved/row-error/progress/inserts-complete/config-saved/done`; client `UdaTaskList` accepts `env` prop, `UdaTaskPage` constructs pgEnv from app+type or `_+_` fallback; verified end-to-end via CSV upload on a pattern with no external pgEnv (2026-04-24)
- [clean-dms-mercury2.md](./tasks/completed/clean-dms-mercury2.md) - Clean dms-mercury2 database: deleted obsolete apps, countytemplate patterns, templated pages, obsolete patterns, extracted images, consolidated history, ran orphan cleanup, VACUUM, prepared for split-app mode (2026-03-17)
- [deprecate-internal-dataset.md](./tasks/completed/deprecate-internal-dataset.md) - Deprecate internal_dataset for internal_table: migration script converting UUID/uppercase doc_types to name-based, moved 117,474 rows from data_items to split tables across 34 datasets, updated source records, phantom ref cleanup, removed internal_dataset from type selector, 25 tests (2026-03-17)

## ssr

- [ssr-basic.md](./tasks/completed/ssr-basic.md) - SSR Phase 1: platform-agnostic core (`render/ssr2/handler.jsx`), Express adapter (`render/ssr2/express/`), `mountSSR()` integration into dms-server (`DMS_SSR` env var), `getSubdomain` fix (window bug + localhost production fix), client hydration via `defaultData`/`hydrationData`, two-build system (Vite client + server), per-host route caching, Lexical SSR fixes, linkedom DOM stubs, 8x faster SSR build (2026-02-27)

## local-first

- [local-first-toy-sync.md](./tasks/completed/local-first-toy-sync.md) - Toy sync engine: standalone notes app proving SQLite WASM (wa-sqlite + OPFS), Yjs conflict resolution, revision-based sync protocol, passthrough pattern, multi-tab reactivity (2026-03-03)
- [toy-sync-lexical.md](./tasks/completed/toy-sync-lexical.md) - Toy sync Lexical integration: replaced textarea with DMS Lexical rich text editor, dark theme provider, Tailwind @source for DMS files, Lexical JSON through Yjs LWW sync pipeline, live two-tab sync via remoteVersion remount, echo suppression fix (pendingItemIds per-item lifecycle), pushUpdate 404→create fallback, idempotent server POST (2026-03-03)
- [toy-sync-collaborative-editing.md](./tasks/completed/toy-sync-collaborative-editing.md) - Toy sync collaborative editing: character-level Yjs ↔ Lexical binding via `@lexical/yjs` CollaborationPlugin, custom ToyProvider over existing WebSocket, room-based WS routing, server-side Y.Doc management + yjs_states persistence, Yjs sync protocol (step1/step2), cursor/presence awareness, user identity (2026-03-03)
- [sync-pattern-scope.md](./tasks/completed/sync-pattern-scope.md)
- [dms-local-first-sync.md](./tasks/completed/dms-local-first-sync.md) - DMS local-first sync integration: REST sync endpoints, WebSocket live notifications, Yjs collaborative editing, pattern-scoped bootstrap/delta, SQLite WASM client (2026-03-16) - Pattern-scoped sync + SQLite fix: chunked SQLite queries (LIMIT/OFFSET + setImmediate yielding), pattern-scoped bootstrap/delta/WS endpoints (?pattern=DOC_TYPE&siteType=Y, ?skeleton=SITE_TYPE), client skeleton bootstrap + on-demand bootstrapPattern(), per-pattern revision tracking, on-demand bootstrap in dmsDataLoader (2026-03-05)

## project maintenance

- [vite-8-upgrade.md](./tasks/completed/vite-8-upgrade.md) - Upgrade to Vite 8: Rolldown replaces esbuild+Rollup, `rollupOptions`→`rolldownOptions`, React Compiler via `@rolldown/plugin-babel` + `reactCompilerPreset`, removed `vite-plugin-top-level-await` (native Rolldown), Yjs dedup alias, dev server 457ms, production build 54s (2026-03-17)
- [package-json-cleanup.md](./tasks/completed/package-json-cleanup.md) - package.json cleanup: removed unused root deps (`@heroicons`, `@hello-pangea/dnd`, `react-redux`, `vite-plugin-top-level-await`, duplicated `compression`/`express`); added missing deps to `@availabs/dms` (`react-dom`, `y-protocols`); bumped `@types/react` 18→19; npm workspaces migration (outer root claims `src/dms/packages/*`, submodule root drops workspaces); deleted `forms_bak/` + dropped `react-table`/`react-popper` (removes all ERESOLVE peer noise, no `.npmrc` hack needed); deleted `componentsIndexTable` + 2 dead `FilterableSearch` files; pinned `@carbon/icons-react@11.76.0` after 11.78 broke Rolldown tree-shaking (~1.4 MB saved minified); added `engines: node>=20.17` to both dms packages; docker verified unaffected (2026-04-17)
- [vite-hmr-fast-refresh-fixes.md](./tasks/completed/vite-hmr-fast-refresh-fixes.md) - Vite HMR / Fast Refresh fixes: all 5 phases — named 24 anonymous default exports + deleted dead `index_bak`/`printWellPdf`; renamed 9 non-JSX `.jsx`/`.tsx` → `.js`/`.ts`; split 19 UI-component theme exports to `*.theme.{js,jsx}` siblings; split 12 pattern-component mixed exports (themes + utils + section_utils + searchUtils/searchConfig + auth context/providers); Lexical editor `createHeadlessEditor` / `uuid` / `TOGGLE_LINK_COMMAND` extracted, `Shared*Context` hooks split; 24 `{EditComp, ViewComp}` object-wrapped exports refactored — columnTypes converted to named exports with dictionary assembly in `index.jsx`, ComponentRegistry entries split into `.config.{js,jsx}` siblings; public `registerComponents()` contract preserved; packages/dms/CLAUDE.md documents the conventions (2026-04-19)

## type system

- [type-system-refactor.md](./tasks/completed/type-system-refactor.md) - Type system refactor: uniform `{parent}:{instance}|{rowKind}` scheme, slug-based instance names replacing UUIDs, `doc_type` eliminated, sources scoped to dmsEnv, `:data` suffix replaces split type regex, `cms-section` → `component`, shared type-utils (74 tests), migration scripts (in-place + site-to-site), all 18 phases complete (2026-04-12)
- [pg-dump-mercury-types.md](./tasks/completed/pg-dump-mercury-types.md) - Clone dms-mercury-2 to dms-mercury-types: pg_dump/restore dms2 → dms_types on mercury, db config for type system refactor work (2026-03-18)

## config

- [unified-config.md](./tasks/completed/unified-config.md) - Unified project configuration: consolidated App.jsx, dms-server .env, and SSR env vars into single root `.env` file read by both Vite (`VITE_*`) and dms-server (2026-02-27)

## ui

- [lexical-link-rel-noopener.md](./tasks/completed/lexical-link-rel-noopener.md) - Reverse-tabnabbing fix (MitigateNY Qualys 150222): `LinkPlugin` created `target="_blank"` links with no `rel`, and the published view renders the same `LinkNode`, so existing links rendered without `rel`. Shipped `SafeLinkNode` — a node-replacement (registered in `PlaygroundNodes.ts`) that forces `rel="noopener noreferrer"` on `_blank` links in `createDOM`/`updateDOM`/`exportJSON`, plus `rel`-at-creation in `LinkPlugin`. Covers **existing** authored content at render time (no migration) because `$createLinkNode`/`importJSON` route through `$applyNodeReplacement`; applies in both the live composer and SSR (`createHeadlessEditor`→`$generateHtmlFromNodes`→`createDOM`). Lexical 0.39 invariants force the replacement to carry its own type, so new/re-saved links serialize as `safe-link` (existing on-disk `link` rows untouched, nothing in-repo keys links by that literal). Verified by a headless `@lexical/headless` round-trip + Playwright against `localhost:5173` (0 `_blank` anchors missing `rel` across the flagged pages, existing content un-resaved). Companion clickjacking fix (150124) = anti-framing headers in `dms-template/netlify.toml`. Content cleanup: the one "malformed link" was a `ButtonNode` ("Learn More") whose `path` held a pasted paragraph on page 1130761 — neutralized `path`→`#` in both draft (1429430) + published (1506581) rows via the DMS CLI on `dmsserver.availabs.org` (live). **Remaining = owner deploys `dms-template` to Netlify (`npm run deploy`) so the bundle + headers go live, then client re-scan** (2026-06-17)
- [design-system-icon-sync.md](./tasks/completed/design-system-icon-sync.md) - Icon set lifecycle: brand-keyed `scripts/icons-audit.mjs` (`<!-- icon: Name -->`/`<!-- decorative -->` page convention; gates on untagged/unregistered) + `scripts/icons-sync.mjs` (design-system `theme/icons.js` → generated live `icons.jsx`; **source of truth = design system**; `--check` CI guard). New skill `managing-design-system-icons.md` + integrated into `designing-a-dms-design-system.md` (icon set = first-class deliverable) and `translating-design-system-to-dms-theme.md`. Captured **all 19 TransportNY design pages** (freight-atlas + the legacy backfill: landing/login/docs/work-zones/congestion/floating-car/getting-started/map-21×3/employment×2); 18 new icons (modes, surfaces, docs, Building/Map/Export); registry 48→67. **Full-repo `icons-audit` green** (255 icon refs, 50 decorative, 0 untagged) (2026-06-09). - `UI.Pill` made theme-driven (`theme.pill.styles`, BC default reproduces today's inline-color look) + `dot` support; transportny pill set to the card design (bordered tint + dot + uppercase), flowing through to `status_pill`; components.html pill section updated (2026-06-08).
- [lexical-button-theme-integration.md](./tasks/completed/lexical-button-theme-integration.md) - Lexical `/Button` slash command moved onto theme/UI: `ButtonNode` decorates via `UI.Button` (`activeStyle = node.__style`, dropped hardcoded `BUTTON_STYLES`); `InsertButtonDialog` uses `UI.Input`/`UI.MultiSelect`/`UI.Switch` from ThemeContext with the style dropdown auto-populated from `theme.button.styles[].name`; `UI.Modal` hosts the dialog. BC: legacy style names fall back to `styles[0]` (2026-06-08).
- [graph-axis-font-theming.md](./tasks/completed/graph-axis-font-theming.md) - avlGraph axis fonts made themeable. `tickFont*`/`labelFont*` (CSS-valued) keys on `xAxis`/`yAxis` flow theme `chartDefaults` → per-section `display` → `GraphComponent` (`axisFontProps` helper) → axis renderers (`AxisLeft/Bottom/Right.jsx` apply via `.style(_, … || null)` — unset removes the inline style so the CSS `0.75rem`/inherit default wins = BC). Default theme (`graph_new/theme.js`) and the transportny brand both define the full 8-key set; default reproduces today's look with `tickColor: currentColor` (also fixes dark-mode ticks), brand = mono 11px slate-500 ticks. 8 text inputs/axis added to the X/Y-Axis control groups; `authoring-graphs.md` documents the tokens. Verified live on §02 + PHED (ticks now mono; line/area/domain unchanged) (2026-06-03)
- [avlgraph-theme-integration.md](./tasks/completed/avlgraph-theme-integration.md) - avlGraph theme integration + §02 trend redesign. Theme-driven `chartDefaults` (merged under per-section `display`) + brand palette + `authoring-graphs.md` skill; calc-series binding fix (resolve by `normalName || name`, `exempt` agg accessor); all visual tokens (axis/grid/line/markers/area) + per-series `interpolation`/`color`/`dashArray`; reference line = a joined target column as a stepped 2nd yAxis series; per-chart header + hero-stat cards on all four §02 trends; real 2027 `applicable_target`s wired (step per year). Axis-font theming spun off to its own task (2026-06-03)
- [avlgraph-author-options-domain-marks-tooltip.md](./tasks/completed/avlgraph-author-options-domain-marks-tooltip.md) - 4 BC author-editable avlGraph options for MAP-21 PM3: custom `yAxis.domainMin/Max` (top=100 on % graphs), per-series `showMarks` dots, `tooltip.yFormat` (stop dropping decimals), `tooltip.showTotal` toggle. Plus the downstream page work: split §02 compound headers replicated to Non-Interstate + Truck, PHED graph added. Default-off/unset preserves the prior render (2026-06-03)
- [map21-section03-align-to-mockup.md](./tasks/completed/map21-section03-align-to-mockup.md) - Aligned MAP-21 §03 "How targets work" to the design mockup: rebuilt the single flowing `lexical` block (2173960) into the mockup's 12-col grid — a `size 12` heading + a `size 7, rowspan 3` two-prong card + three `size 5` stacked cards (2174048–2174051), reordered contiguous in `draft_sections`. **No theme/component change required** — card chrome came from the per-section `border:"full"` (= `theme.sectionArray.border.full` rounded+border+bg+shadow) + `padding`; colored prong/legend dots from Lexical text-node inline `style` (`color:…`); content reconciled to the mockup incl. the restored Performance-periods (P2/P3) card. Lettered A/B badges approximated as color-coded dots (no inline lexical badge node exists). Insights captured to `creating-pages-from-a-design-pattern.md` (§5.6.9 rowspan asymmetric grid, §5.6.10 chrome-via-styles-not-Lexical) and `translating-design-system-to-dms-theme.md` (gotcha #5 + §3.1.57: Tailwind-v4 leading-zero `/05` opacity is invalid) (2026-06-01).
- [spreadsheet-flexible-column-widths.md](./tasks/completed/spreadsheet-flexible-column-widths.md) - Spreadsheet (CSS-grid table) now lets fixed-width and flex-stretch columns coexist: `augmentColSizing` in `table/index.jsx` gives each column a `_track` (explicit `size` → `${size}px`; unsized → `minmax(${defaultColumnSize}px, 1fr)`) and `_hasFixedSize`; Header/Row/add-new templates join `_track`, and `TableCell` pins `width` only for fixed columns (flex cells fill their track). Fixes the under-fill gap when a narrow fixed column (e.g. a 56px `#`) leaves the grid short of the container. BC — resize writes `column.size` so it pins to fixed; wide tables floor at the auto-fit `defaultColumnSize` and still overflow-scroll. O(cols), no per-row cost. Verified live: MAP-21 §04 fills width with header/body aligned; §06 (30 cols) still scrolls (2026-06-01).
- [lexical-plugins-ui-migration.md](./tasks/completed/lexical-plugins-ui-migration.md) - Migrated the five remaining Lexical plugin/node dialogs (ActionsPlugin, InsertLayoutDialog, InlineImagePlugin, TablePlugin, InlineImageComponent) off the lexical-internal `ui/{Button, Select, TextInput, Dialog}` primitives onto `UI.{Input, Button, Select, Switch, DialogActions}` from `ThemeContext`. Deleted orphans `lexical/editor/ui/{Button, Select, Dialog}.tsx`; left `TextInput.tsx` in place since the lexical-internal `ColorPicker.tsx` (out-of-scope) still consumes it (2026-05-24).
- [ui-dialog-actions-primitive.md](./tasks/completed/ui-dialog-actions-primitive.md) - Lifted the dialog action-bar wrapper out of `lexical/editor/ui/Dialog.tsx` into shared `UI.DialogActions` (`components/DialogActions.{jsx,theme.js}`, registered on `UI` + `defaultTheme`). Tessera aliases `dialogActions.wrapper` to `modal.footer` so action bars match the modal's hairline bottom-rule treatment. Companion: shipped `UI.Select` — a thin `singleSelectOnly` wrapper around `UI.MultiSelect` — so the just-migrated dialogs read `<Select options value onChange>` instead of repeating the `<MultiSelect singleSelectOnly searchable={false} value={x ? [x] : []} onChange={s => …Array.isArray…}>` boilerplate at every single-value site (2026-05-24).
- [lexical-layout-templates-theme-driven.md](./tasks/completed/lexical-layout-templates-theme-driven.md) - Lifted `InsertLayoutDialog.tsx`'s hardcoded `LAYOUTS` array out to `theme.lexical.layoutTemplates`; the dialog reads from `editor._config.theme.layoutTemplates` (codebase `LAYOUTS` as fallback). Tessera ships 6 brand-appropriate templates including a `2 buttons side-by-side` content-width preset for the hero CTAs, plus `layoutItem` / `layoutContainer` overrides that drop the codebase default's heavy `px-2 py-4` padding inside each column. (2026-05-24)
- [mapeditor-migrate-off-headlessui.md](./tasks/completed/mapeditor-migrate-off-headlessui.md) - Migrated `patterns/mapeditor/` + sibling `patterns/page/.../map_dama/` off `@headlessui/react` onto the in-house UI kit via `ThemeContext`. 7 phases shipped (consumer count 22→0 across the dms submodule): P0 dead imports (2 files); P1 standalone `<Dialog>` → `UI.Modal` (6 files, also fixed latent themeOptions bug on SaveChangesMenu's Headless Button); P2 slide-in Dialog + command palette → `UI.Modal` + `UI.Input` (2 files; `afterLeave` query reset replicated via `useEffect`; CommandPalette primitive proposed but not built — single consumer reduces cleanly); P3 `<Popover>` → `UI.Popup` (2 files); P4 `<Menu>` → `UI.NavigableMenu` for action menus / `UI.Popup` for "menu-as-wrapper" (LayerInfo info cards, DynamicFilterControl checkbox-list pickers — 5 files); P5 `<Tab>` → `UI.Tabs` clean (1 file) or inline ARIA tabs where the tab list has sibling content / tabs are derived from runtime state (4 files: LayerManager, ExternalPluginPanel, InternalPluginPanel, map_dama/MapManager); P6 `<Combobox>` → plain `<input>` + suggestion list (1 file; not really a Combobox in the first place); P7 dependency removed from `packages/dms/package.json`. Visual regressions accepted per the durable feedback memories ("Headless perf > a11y", "no className passthroughs on UI primitives") — call sites recover via `activeStyle` / named theme styles, not per-call overrides. Broader transportNY repo (DataManager, freightatlas) still imports Headless directly — out of scope, downstream cleanup (2026-05-14)
- [ui-consolidation-drop-headlessui.md](./tasks/completed/ui-consolidation-drop-headlessui.md) - Removed `@headlessui/react` from every component in `ui/components/`. 7 phases: P0 dead-code/free wins (delete `SideNavContainer`, stray `components/index.jsx`; drop unused imports from `List`/`ButtonSelect`; drop unused `UI.ColorPickerFlat` registry entry — 14→11 consumers); P1 cluster consolidation (delete `Popover` w/ 0 callers; `Menu`→`NavigableMenu` 3 sites; `Dropdown`→`NavigableMenu` 4 sites; move `Permissions`→`patterns/admin/components/` — 11→9); P2 thin-wrapper → native HTML (`Button`/`Input`/`Textarea`/`FieldSet` with `useId()` for label-input wiring; theme `data-[*]:` → native `hover:`/`focus-visible:`/`disabled:`/`aria-invalid:` — 9→5); P3 in-house Dialog family via `createPortal` + shared `useModalOverlay` hook (Escape + scroll-lock, no focus trap; `Dialog`/`Modal`/`Drawer`/`DeleteModal` rewritten — 5→1); P4 click-only Tabs with `aria-selected` ARIA — 1→0. **Package not yet uninstalled**; 22 mapeditor/map_dama files still import directly. Follow-up: `mapeditor-migrate-off-headlessui.md`. Locked in 2 durable design feedback memories: "Headless perf > a11y tradeoff" + "no className passthroughs on UI primitives" (2026-05-14)
- [selection-components-consolidation.md](./tasks/completed/selection-components-consolidation.md) - Consolidated `Listbox` / `Select` / `ComboBox` / `ColumnTypes.multiselect` into a single `UI.MultiSelect` primitive at `ui/components/MultiSelect.{jsx,theme.js}` (column-type Edit/View wrappers re-import from there). All FieldSet `type: 'Select'` configs (30) and direct `<Select>` JSX usages (17 across 10 files) migrated; lone `<ComboBox>` site migrated; both ControlRenderer default-onChange handlers updated to accept event-or-value. Deleted `Select.jsx`, `ComboBox.jsx`, the unused `columnTypes/select.jsx`; removed from `UI` registry, FieldSet componentRegistry, defaultTheme, and docs. New Catalyst-aligned theme (inset ring, focus-within, dark mode); portal-based menu render escapes ancestor `transform` stacking contexts; React-element labels fall back to `value` for search filter (2026-05-14)
- [fix-theme-array-merge.md](./tasks/completed/fix-theme-array-merge.md) - Implemented `mergeTheme` with `_replace` convention so array fields in themes get replaced instead of deep-merged (2026-01-28)
- [move-widgets-to-top-level-theme.md](./tasks/completed/move-widgets-to-top-level-theme.md) - Moved widgets from layout.options to top-level theme property (2026-01-29)
- [redesign-widget-system.md](./tasks/completed/redesign-widget-system.md) - Redesigned widget system: key/value format, combined definitions + components in `ui/widgets/index.jsx`, additive merge, bridge for runtime registration (2026-01-29)
- [lexical-phase0-analysis-cleanup.md](./tasks/completed/lexical-phase0-analysis-cleanup.md) - Lexical Phase 0: dead code cleanup (~28 files), upstream comparison, table plugin updates (hover feedback, Mac scrollbar handling) (2026-02-03)
- [lexical-plaintext-normalize.md](./tasks/completed/lexical-plaintext-normalize.md) - Moved plaintext-to-Lexical-JSON conversion into shared `parseValue()` so both editor and HTML view paths handle plaintext input (2026-02-08)
- [lexical-sync-html-render.md](./tasks/completed/lexical-sync-html-render.md) - Eliminated Lexical View jitter: added sync `getHtmlSync()` using `editorState.read()`, switched View from `useEffect+useState` to `useMemo`, restored collapsible handlers (2026-02-08)
- [theme-merging-issues.md](./tasks/completed/theme-merging-issues.md) - Fixed styles array cross-contamination, standardized component theme default fill-in via `getComponentTheme()` (2026-02-09)
- [map-component-refactor.md](./tasks/completed/map-component-refactor.md) - Map component refactor: extracted core map from avl-map-2 into `ui/components/map/` (6 files from 25+), dynamic maplibre-gl import for code splitting, removed dead UI/theme system/npm deps, migrated all consumers to ThemeContext UI pattern, removed avl-map-2 and avl-components submodules, deleted avl-map-2_bak and dms_OLD, converted all lodash→lodash-es imports (2026-03-01)

## patterns

### patterns/page

- [spreadsheet-component-templates.md](./tasks/completed/spreadsheet-component-templates.md) - Component Templates ("copy/paste from the DB"): save a configured **Spreadsheet** section as a named, reusable template persisted as an ordinary `data_items` row (`app`=site app, `type`=`${pattern}|${componentSlug}_template`; name is a data attribute → "list my component type's templates" is one exact-type `apiLoad`). New `TemplateManager.jsx` menu panel + `template_utils.js` (pure) + `TemplateManager.theme.js`, gated behind a registry `supportsTemplates` flag (Spreadsheet first; Card/Graph = one line). **Zero server changes** — create/list/edit/delete ride the generic Falcor `dms.data.*` paths. Two author toggles (both default ON) fully partition the saved state: *Include data source* = the whole data config (`externalSource`/`join`/`columns`/`filters`/`customBuckets`/`pivot`), *Include layout* = section chrome (`extractLayout`) + `state.display` (the Spreadsheet Settings menu writes there via `dwAPI.setDisplay`). **Apply** = `onChange(new element-data + layout + provenance)` + a unique `setKey()` to remount the dataWrapper (edit-mode has no `[value]` resync; `components/index.jsx` keys `DataWrapper.EditComp` on `compKey`); for any excluded bucket it falls back to the section's live `dwState`, and always leaves an `externalSource` present so `migrateToV2` doesn't discard columns to defaults. **v1 = Save+Load; v1.1 = full CRUD** — overwrite reuses the row id & preserves `createdAt`/`createdBy` while bumping `updatedAt`/`updatedBy` (makes drift real), delete via `requestType:'delete'` + optimistic removal, inline two-step confirm guards (no modal). Per-field provenance `_appliedTemplate.fields[path] → {templateId, templateName, templateUpdatedAt (=row `data.updatedAt`), appliedAt}` lets different templates own different fields. `createdBy`/`updatedBy` stored for future auth/ownership. Lint clean (only the repo-baseline `react/prop-types`). **Phase 3 (drift detection / "template updated, refresh?") deferred** — full spec retained in the task file's Phase 3 section + tracked as a standalone `todo.md` entry; provenance plumbing already ships. Verified in UI (2026-06-11)
- [theme-driven-default-section-groups.md](./tasks/completed/theme-driven-default-section-groups.md) - New pages are now seeded with their default section groups (incl. a `sidebar` group) from a **sane simple pages-theme setting** `pages.sectionGroup.defaultStyle` (default `'content'`). The de-facto creator — the lazy `sectionsEditBackill` (`newPage` makes a bare page) — now reads the theme style + **always adds a `sidebar` group** so the in-page-nav rail has an author-reachable content area on every page; content group stays `name:'default'` (BC: the `SectionArray` ungrouped→default fallback). `sectionGroup.jsx` synthesizes a `sidebar` group for pages predating this (no migration), and the rail renders whenever `item.sidebar` is on (gating falls back to the first content band) — so the rail shows before any `navLabel` exists. Section-groups pane still hides `position:'sidebar'` by design. Verified: throwaway page → `[{default,content},{sidebar,sidebar}]`; empty page renders the 302px rail; map_21 regression clean (2026-06-09).
- [in-page-nav-themeable-sidebar.md](./tasks/completed/in-page-nav-themeable-sidebar.md) - Themeable, author-toggleable "on this page" rail. `item.sidebar` toggle (left/right); per-section `navLabel`/`anchorId` opt-in **decoupled from title/level** (empty-title `h2` headers participate) + clean anchor id (legacy `#Title` alias kept); new `InPageNav` rendered from `pages.sectionGroup` theme (not `UI.SideNav`) + `useScrollSpy` active state; custom rail content via a `sidebar` section group (D2). **Design pivot:** the content↔rail two-column layout is owned by the **pages theme** (`pages.sectionGroup.contentRow/contentCol`, rendered inside the band), not the shared `layoutGroup` style — `items-stretch` is load-bearing for the inner sticky. Rail attaches to the band of the first `navLabel` section (`'default'` fallback = BC). Verified live on `map_21` (2173915): 6 nav items, scroll-spy, related card, sticky pin at `top-[60px]`. Skill: `adding-an-in-page-nav-rail.md` (2026-06-09).
- [card-header-alignment-and-casing.md](./tasks/completed/card-header-alignment-and-casing.md) - Per-column Card **header** controls: `headerJustify` (the header label was an inline span, so `text-*` was inert on it) + `headerCase` (was hardcoded `capitalize`), BC-by-default (unset = today's behavior). Surfaced transcribing the MAP-21 interstate KPI card (left-aligned, sentence-case title) (2026-06-08).
- [map21-kpi-column-types.md](./tasks/completed/map21-kpi-column-types.md) - Three value-driven, theme-registered column types in transportny — `status_pill`, `target_bar`, `delta` — for the MAP-21 KPI mockup's rung-3 "look depends on the value" cells the Card grid couldn't express. Purely additive → BC (2026-06-08).
- [graph-color-schemes.md](./tasks/completed/graph-color-schemes.md) - graph_new color-scheme selector: Observable Plot / d3 scheme catalog (categorical / diverging / sequential / cyclical) + reverse in a category-grouped `colors` control group; persists `{type:'scheme',name,reverse}` while keeping the legacy `{type:'palette',value:[...]}` shape (BC); AVAIL palettes kept under "Custom"; default `observable10`; schemes resolved via d3 (no `@observablehq/plot` runtime dep) (2026-06-08).
- [section-height-setting.md](./tasks/completed/section-height-setting.md) - Section-level height select (`auto`/`fill`/`hero`/theme presets) under the section menu Layout group; presets in `section.theme.heights`; `section.jsx` applies inline height + flex-column only when non-`auto`. Strictly BC — under default `auto` the rendered HTML is byte-identical to before. Gives fill-wanting descendants (`portrait_banner: bannerHeight:'fill'`) a parent height to resolve against (2026-06-08).
- [datawrapper-join-support.md](./tasks/completed/datawrapper-join-support.md) - DataWrapper multi-source joins: UDA configs compiled to server-side SQL with WITH/JOIN, a join DSL, and join UI in the data-sources pane (2026-06-08).
- [landing-page-translate.md](./tasks/completed/landing-page-translate.md) - Built the TransportNY marketing landing page (`dms_design_system_v2/pages/landing.html`) as 18 draft sections across 5 bands in the `landing` pattern (1700630, page 2173051), theme transportnyv2 (user-set). Decision: **everything as sections**. Seed script `scripts/seed-landing-page.mjs`. Bands → layoutGroup: Top nav (`header`/top — brand + nav links + Sign-in/Get-started via lexical layout-container), Hero (`hero` tny-hero-topo — kicker + displayHero h1 w/ amber period + proseLG + 2 CTAs + status row, plus a `border:full` CURRENT COVERAGE stat card), Products (`content` #ECEEF2 — intro + 5 `border:full` cards 4/4/4 then 6/6), For whom (`content_tint` #E4E8EE — size-7 **rowspan-3** copy + 3 size-5 audience cards), Footer (`footer`/bottom — brand/products/resources/status + hr colophon). Strong transportnyv2 token coverage (kicker/displayHero/displayLG/prose*/meta*/displayXS + buttons default/secondary/plain; card chrome via `border:'full'`). Verified live at `http://localhost:5173/edit/landing`. **Key reusable gotcha (rolled into `creating-pages-from-a-design-pattern.md`):** multiple patterns share `base_url:/`; a `subdomain:null` pattern is the default/fallback and resolves on the **bare host**, not `npmrds.localhost` (which `npmrds_sub` owns) — and pass `--pattern <id>` on `section create`. Logged ask-first gaps: no lexical node for styled icon boxes / NY brand mark; would-add tokens (64px display, mono-tabular stat) (2026-06-03)
- [map21-single-page-dms-build.md](./tasks/completed/map21-single-page-dms-build.md) - Built the MAP-21 PM3 single-page report (`map-21-system-performance.html`) as a live draft DMS page in `npmrdsv5+npmrds_sub`, driven by a single `year_record` page variable. All 8 phases shipped & verified live: §01 four KPI compliance cards (2173919–22 — status_pill / target_bar / delta column types), §02 reliability-over-time trends on `avlGraph` (stepped target reference lines, 2173963–65), §03 "how targets work" lexical/grid explainer (2174048–51), §04 regional MPO compliance matrix with per-cell verdict dots + `Met X/3` roll-up (2173961), §05 urban-congestion UZA spreadsheet w/ two-key (`year_record`+`urban_code`) target join (2173962), §06 annual download spreadsheet (2173959). Primitive enrichments landed along the way: built-in `status_pill`/`target_bar`/`delta` column types + themeable `UI.Pill`, Graph stepped-reference-line + hover-label precedence + x-axis padding default, per-section "Card style"/"Table style" named-style pickers, transportny `theme.table` re-key. Deliverable skill `creating-interactive-pages.md` written + indexed. Draft-only (humans publish). **Closed at higher-priority request with cross-cutting polish deferred** (§01 roll-up header, KPI superscript units, per-section band tints, sticky-TOC chrome, MPO/County group-by toggle, §05 per-capita/Non-SOV blocked on UZA population data) — tracked in the task's deferred/ledger sections (2026-06-03)
- [custom-buckets.md](./tasks/completed/custom-buckets.md) - Custom buckets: an author-defined GROUP BY dimension that maps a source column's values into named groups, configured entirely from the dataWrapper section menu (bound to page filters for dynamic groups, or set statically). **Server:** new `buildAliasGroupCase(definition)` in `clickhouse.js` compiles `{ column, fallback, groups: { [label]: [values] } }` into a `CASE WHEN <column> IN (...) THEN '<label>' … ELSE '<fallback>' END` (column `sanitizeName`-guarded, literals single-quote-escaped, JSON-stringified value arrays tolerated); `simpleFilter`/`simpleFilterLength` substitute the CASE into GROUP BY + SELECT for any alias present in `groupBy`, and the `array_agg(distinct …)`→ClickHouse regex was widened to match a CASE. **Client:** `resolveAliasGroups` (in `usePageFilterSync.js`, the sole config-recompute path, guard fixed so static buckets resolve without page filters) compiles the UI config into `customBuckets.config`, which `buildUdaConfig` passes through as `options.aliasGroups`; a synthetic `origin:'custom-bucket'` column represents the dimension and is owned solely by an explicit `dwAPI.reconcileCustomBucketColumn()` action (renames not duplicates, fired on Dimension Alias blur/Enter via a draft-state `CustomBucketAliasInput`) — the old dual-purpose `useEffect` in `dataWrapper/index.jsx` (Edit+View) was removed. **Filter to buckets** toggle (default ON): exported pure `buildCustomBucketFilters(customBuckets, baseSourceId)` emits one `{ col, op:'filter', value:[…uniqueValues], source_id }` leaf per alias (deduped union of group arrays, fallback/null/empty excluded, stringified arrays JSON-parsed), injected at step 5 wrapping the tree in `{ op:'AND', groups:[existingTree, …bucketLeaves] }` so it AND-restricts regardless of the user's filter relation; `source_id` aliases the leaf to `ds.` under joins; server already compiles `op:'filter'`→`IN (...)`. **Master switch** `customBuckets.enabled` (**default OFF** via an `enabled === true` gate — buckets do nothing until flipped on, even after configuring): off removes the synthetic column and gates both `aliasGroups` and `buildCustomBucketFilters`, while retaining `customBuckets.config` for clean re-enable; `sectionMenu.jsx` shows On/Off in the header and hides sub-config while off. Custom Buckets menu refactored to the JSON `setCbConfig` pattern. Unit tests green (127/127). Live-verified (master-switch live verify pending) (2026-06-08)
- [datawrapper-blank-row-fallback.md](./tasks/completed/datawrapper-blank-row-fallback.md) - dataWrapper blank-row fallback for empty results: opt-in via `display.useBlankRowFallback`. When the section opts in and `getData.js` sees `length === 0`, synthesize a single placeholder row keyed by `column.normalName || column.name` from each visible column's `blankDefault`, tagged `_isBlankFallback: true`. Section-level toggle ("Render Blank Row When Empty") under `controls.data`; per-column "Empty Default" inHeader entry mounts the column type's existing `EditComp` so any column type (text, calc-text, portrait_banner, image, …) gets a type-appropriate authoring widget. Calc columns store the literal final value — no SQL re-evaluation. BC: defaults to false, sections that haven't opted in return `{ length: 0, data: [] }` exactly as before. **Live-test fix (2026-05-18):** original synthesis sat at the function tail but was unreachable — `getData.js` exits earlier through `if (fromIndex >= length) return { length, data: [] }` whenever `length === 0` (0 ≥ 0). Moved synthesis inside that guard, removed the dead tail block. WCDB show card 1964234 rendered blank during an off-air slot until this fix landed (2026-05-18)
- [permissions-refinement.md](./tasks/completed/permissions-refinement.md) - Permissions refinement: consolidated `edit-page-layout`/`edit-page-params` into `edit-page`, removed `edit-section-permissions` (moved to page-level `edit-page-permissions`), renamed section permission `edit-section`→`edit` (backward-compat alias kept in checks), gated Pages pane on `create-page`, copy/link pills visible to any user with page `edit-page`, move up/down and Permissions item require section `edit`, `PublishButton` respects page-level authPermissions override, edit page redirects logged-in users without edit access to view page (2026-05-20)
- [card-combine-format.md](./tasks/completed/card-combine-format.md) - Card column-combining formatFn: new `combine` entry in `formatFunctions` with signature `(value, row, attr)` — reads `combineWith` (sibling column name) and `combineSeparator` (default `' — '`) off the column attr to render two row fields on one editorial line (e.g. "Eternal Life — Jeff Buckley"). `Card.jsx` routes `combine` through a dedicated branch alongside `icon`/`color` so it gets `source` (the row) and skips the trailing `.replaceAll(' ', '')` that would eat separator whitespace. Format dropdown extended with "Combine columns"; conditional `Combine With` + `Separator` text inputs appear in the column toolbar when selected. `card-layout.md` extended with the format-functions table entry, the recipe (image + combined title + album + hidden artist loader), and quick-reference keys. WCDB section 1963473 collapses song/artist into one cell with album below; `album_cover`/`play` `cellRowSpan` dropped to 2 to bracket the now two-row text stack; `artist_name` reduced to a hidden full-row loader so the query still fetches it. Driving design rule: themes/CLAUDE.md explicitly names "a column-combining formatter that renders `album · year` from two fields" as a legitimate Card enrichment (2026-05-18)
- [card-cell-padding-overrides.md](./tasks/completed/card-cell-padding-overrides.md) - Card per-column padding overrides: extended the existing `cellPaddingBottom` knob to `cellPadding` (all sides) + `cellPaddingTop` / `cellPaddingLeft` / `cellPaddingRight`. New `padOverride()` helper in `Card.jsx` ~425 distinguishes "field cleared / unset" (fall through to ambient `cellsPadding`) from "author typed 0" (apply 0), so a per-column `cellPaddingTop: 0` is honored. Precedence: side-specific > `cellPadding` > section-level `cellsPadding`. Five `isBatchUpdatable` numeric inputs added to `Card.config.jsx` per-column toolbar, clustered next to the existing Padding Below. `card-layout.md` per-cell visual overrides table extended; quick-reference includes the four side keys. Driving case: WCDB now-playing card title/artist/album cells with `cellPaddingTop: 0, cellPaddingBottom: 0` so the text stack collapses inside the 96px album-art height (2026-05-18)
- [card-cell-width-tracks.md](./tasks/completed/card-cell-width-tracks.md) - Card cells grid: per-column `cellWidth` + section-level `cellsTracksTemplate`. New track-cursor walker in `Card.jsx` composes `gridTemplateColumns` from per-column `cellWidth` claims (first-wins on conflicts; collapses additional spanned tracks to 0px when `cellSpan > 1` so the cell renders at the typed width). Section-level `cellsTracksTemplate` raw `grid-template-columns` string is the power-user escape hatch. Per-column "Cell Width" text input added to the section toolbar next to Col Span / Row Span; "Track Template" input added under the Cells Grid section-menu group. First application of the author-empowerment principle (now in top-level `CLAUDE.md`) — WCDB now-playing section laid out with `cellSpan: 1 + cellRowSpan + cellWidth` for the fixed cells. Documented limitation: CSS Grid bakes `cellsGridGap` inside a span, so pixel-perfect width with `cellSpan > 1` requires `cellsGridGap: 0` (or use `cellSpan: 1 + cellRowSpan` for vertical extent). Skill `card-layout.md` extended with a "Sizing tracks" section + the album-art-fixed + text-fluid recipe (2026-05-18)
- [card-unified-grid-layout.md](./tasks/completed/card-unified-grid-layout.md) - Card unified grid layout: collapsed `compactView` row/cell modes into one mode where both cards-grid (outer) and cells-grid (inner) are always real CSS grids with independent settings (`cardsGridSize`/`cardsGridGap`/`cardsPadding`/`cardsBgColor`/`cardBorder` + `cellsGridSize`/`cellsGridGap`/`cellsRowHeight`/`cellsPadding`/`cellBorder`). One-shot read-time migration (`Card.migrate.js`, dispatched from `migrateToV2.js` for `compName === 'Card'`) routes the legacy single setting set onto the matching grid, defaults the unused axis to 1-column, and strips `compactView` + legacy display/column keys. Per-attribute keys renamed (`cardSpan`→`cellSpan`, `cardRowSpan`→`cellRowSpan`, `bgColor`→`cellBgColor`, `pb`→`cellPaddingBottom`, `borderBelow`→`cellBorderBelow`). Editor's "Each Card Represents" select removed; mode-flipping border toggles replaced with two clean ones inside the grid groups. New `image` column type with file-upload via the same `dama-admin/:pgEnv/file_upload` API as InlineImage; legacy text-column image controls retired from the editor (text columns with `isImg` saved data still render). Side fix: `sectionMenu.jsx` `transformControlItem` now recurses on grouped items so function-typed controls (color picker) inside groups don't crash NavigableMenu (2026-05-07)
- [card-row-span-and-custom-column-types.md](./tasks/completed/card-row-span-and-custom-column-types.md) - Card row span + theme-registered column types + WCDB DJ portrait card: `cardRowSpan` (now `cellRowSpan`) per-attribute, `theme.columnTypes` auto-registration via page-pattern `siteConfig.jsx`, `cardHints` mechanism for column types to opt out of field chrome (`fullBleed`, `spanFullColumns`, `spanFullRows`, `height`), WCDB `portrait_banner` column type + DJ portrait card section as proof-of-concept (2026-05-07)
- [pattern-preload-setting.md](./tasks/completed/pattern-preload-setting.md) - Pattern-level preload toggle: `preload_data` boolean on pattern format (default false), Switch in Overview pane (page patterns only), gated `preload` registration in page `siteConfig.jsx` so historical patterns stay off; also un-commented `dmsConfig.preload(...)` in `dmsPageFactory.jsx:36` (pre-existing artifact that meant preload never ran) (2026-05-06)
- [pivot_table.md](./tasks/completed/pivot_table.md) - Pivot table support for DataWrapper/Spreadsheet: pivot config state + save/load (Phase 1), `usePivotDistinctValues` hook (Phase 2), CASE column SQL generation in `getData` (Phase 3), `setPivot` API (Phase 4), SectionMenu pivot block (Phase 5), ColumnManager pivot columns section (Phase 6), autoResize fix (Phase 7). Post-ship: optional row column (no GROUP BY → single aggregate row); multi-column pivot with cartesian product CASE columns (`pivotColumns: string[]`); dedup via `computePivotFetchKey` + `lastFetchKeyRef`; `pivot_col` columns persisted with sizes so headers show on load and user resizes survive re-injection; grouped multi-row headers in `TableHeader.jsx` (`computePivotGroupRows` + `grid-column: span N`, resize-safe); Single Header toggle (`singleHeader` state field) to switch between grouped and flat compound labels (2026-05-09)
- [consolidate-page-history.md](./tasks/completed/consolidate-page-history.md) - Consolidate page-edit history: replaced per-edit `data_items` rows with single `page-edit` row per page holding `entries[]` array; updated format, editFunctions, historyPane; migration script for existing databases (2026-03-11)
- [table-card-react-router-links.md](./tasks/completed/table-card-react-router-links.md) - Replaced `<a href>` with React Router `<Link to>` for internal navigation in TableCell and Card components; external links use `<a>` with `target="_blank"` and `rel="noopener noreferrer"` (2026-02-25)
- [fix-nav2level-baseurl.md](./tasks/completed/fix-nav2level-baseurl.md) - Fixed nav2Level failing for non-root baseUrl patterns; moved to page pattern as resolveNav callback (2026-01-29)
- [combine-datasources-task.md](./tasks/completed/combine-datasources-task.md) - Combined `pgEnv`, `damaBaseUrl`, and `datasetPatterns` into unified `datasources` array (2026-01-22)
- [lexical-controls-to-config.md](./tasks/completed/lexical-controls-to-config.md) - Moved Rich Text inline controls (style, bgColor, showToolbar) to NavigableMenu config with nested submenu pattern; added ColorPickerFlat component (2026-02-03)
- [theme-component-registration.md](./tasks/completed/theme-component-registration.md) - Extracted component registry to standalone module (`componentRegistry.js`), added `theme.pageComponents` auto-registration in page siteConfig, fixed NavigableMenu `flattenConfig` key collision bug; updated component-overview.md (2026-02-05)

### patterns/datasets

- [datasets-datasources-migration.md](./tasks/completed/datasets-datasources-migration.md) - Migrated datasets pattern to use unified `datasources` array from context instead of `pgEnv`/`damaBaseUrl` (2026-01-23)
- [datasets-pattern-modernize.md](./tasks/completed/datasets-pattern-modernize.md) - Modernized datasets pattern: combined 3 configs, defaultTheme, context-only wrapper, per-page Layout, LayoutGroup + Breadcrumbs, UI components in DatasetsList, dead code cleanup, MetadataComp/ValidateComp theming (2026-02-07)
- [datasets-list-style-cleanup.md](./tasks/completed/datasets-list-style-cleanup.md) - DatasetsList style cleanup: transparent container, theme-driven card/sidebar styling, sticky header+sidebar, full-height layout, design pass, module-level sources cache, React.memo + useMemo performance (2026-02-08)
- [fix-internal-source-blank-page.md](./tasks/completed/fix-internal-source-blank-page.md) - Fixed blank page on internal_source pages where UDA `source_id` attribute was unset; `getSourceData` now falls back to URL param ID (2026-02-09)
- [source-overview-cleanup.md](./tasks/completed/source-overview-cleanup.md) - Source overview cleanup: theme-driven styling (sourceOverview + sourcePage themes), tab jitter/active state fix, loading flash fix, width constraint, column display_name + name, versions table with downloads, SourcePage rename + theme split (2026-02-09)
- [datasets-create-page.md](./tasks/completed/datasets-create-page.md) - Datasets create page: extracted modal create flow into dedicated `/create` route with Layout/Breadcrumbs, type selector, external create component support, clone flow; replaced Add button with Link on DatasetsList (2026-02-09)
- [datasets-settings-page.md](./tasks/completed/datasets-settings-page.md) - Datasets settings page: category visibility settings with two-column toggle, filtered/all toggle on list page, settings gear link for authed users, category breadcrumbs + sub-category nesting in sidebar, no-category sources filtered (2026-02-09)
- [fix-dataset-listing-bug.md](./tasks/completed/fix-dataset-listing-bug.md) - Fixed dataset listing bug: getSitePatterns LIKE query, dmsSiteFactory siteType, DatasetsList category filter for uncategorized sources, UDA cache invalidation (2026-02-13)
- [internal-dataset-admin-page.md](./tasks/completed/internal-dataset-admin-page.md) - Custom admin page for internal datasets: version creation via DMS item.id, SourcePage datatype admin overrides, auto-navigate to latest view, overview shows versions (2026-02-13)
- [internal-table-datatype.md](./tasks/completed/internal-table-datatype.md) - `internal_table` dataset type: combined creation + upload, auto-creates first version, split tables for per-version data (2026-02-23)
- [fix-upload-updatemetadata.md](./tasks/completed/fix-upload-updatemetadata.md) - Fixed `updateMetaData` in upload component: corrected apiUpdate to use source type format and UDA update path (2026-02-23)
- [datasets-source-delete.md](./tasks/completed/datasets-source-delete.md) - Source delete (DAMA): server-side `softDeleteSource` (drops `data_manager.views`+`sources` rows in a transaction) and `hardDeleteSource` (also drops per-view data tables, removes download files via `storage.remove`, deletes `data_manager.tasks` rows for the source), Falcor CALL routes `uda.sources.delete` / `uda.sources.hardDelete`, `DeleteDamaSourceBtn` 3-option modal (Cancel / Delete / Hard Delete) with name-typed confirmation, busy state, error surfacing, Falcor cache invalidation. Internal-table counterpart split out as `internal-table-source-delete.md` (2026-04-24)
- [internal-table-source-delete.md](./tasks/completed/internal-table-source-delete.md) - Internal-table source delete + dmsEnv ownership: `deleteInternalSource` server primitive (drops per-view split tables, strips source refs from any owning dmsEnvs, deletes view rows + source row + dms.tasks rows in one transaction; best-effort cleanup when source row already gone), `softDeleteSource`/`hardDeleteSource` branch on `isDmsEnv(env)` so the existing routes work for both backends, client wiring in `internal/pages/admin.jsx` with broader cache invalidation including each dmsEnv reported updated, `cleanup-stale-dmsenv-refs.js` belt-and-suspenders script with per-app PG schema discovery (registered as `db:cleanup-stale-dmsenv-refs`). Root-cause fix for the "source appearing twice in the page picker": `buildDatasources` now dedupes internal datasources by `pattern.dmsEnvId` so two patterns sharing a dmsEnv produce one queryable env (2026-04-24)
- [internal-pgenv.md](./tasks/completed/internal-pgenv.md) - Internal pgEnv (dmsEnv): new `dmsEnv` row type owns dataset sources instead of the datasets pattern itself, so multiple patterns can share one source pool. `DmsEnvConfig` in pattern editor selects/creates the env; `pattern2routes()` builds per-pattern `datasources` via `buildDatasources(pattern)` resolving the pattern's `dmsEnvId`; `CreatePage`/`internal_table/sourceCreate.jsx` add new source refs to the dmsEnv. `migrate-to-dmsenv.js` groups patterns by source set, creates dmsEnvs, sets `dmsEnvId`, removes `data.sources` from patterns (idempotent, dry-run by default). Migrated-dataset compat fixes: case-insensitive `NAME_SPLIT_REGEX` + doc_type lookups (UDA + dms controllers), `sanitize()` in new split-table naming across all 3 paths, lowercase type for split queries, Falcor `maxPaths` 50K → 500K, `--max-http-header-size=1MB`, `rename-split-tables.js` (39 tables renamed on dms-mercury-2). Invalid-entry table consolidation: stripped `_invalid` suffix so valid + invalid rows share one table — fixes `getRowsByTypes` missing invalid rows on revalidation and `batchUpdateType` leaving rows in the wrong physical table; 137 table-splitting tests pass. Research doc at `planning/research/dmsenv-datasets-uda.md` (2026-04-24)
- [file-upload-dms-backed.md](./tasks/completed/file-upload-dms-backed.md) - `file_upload` → DMS-backed source metadata: new `POST /dms-admin/:app/file_upload` handler stores source/view rows in `data_items` under a `dmsEnv` (owner fallback to `parent` for legacy patterns), storage keyed on `dms-{app}_env-{owner_instance}_s-{id}/v-{vid}/{name}`; server derives source slug from the row's `type` column on append. Client `file_upload/CreatePage.jsx` retargeted to the new route; `ViewPage.jsx` split into `DmsView`/`LegacyView` branched on `isDms`. Works on DMS-only projects without a pgEnv; becomes the default even when a pgEnv is configured. Production-verified by uploading 10 MP3s into the `asm+nhomb` songs source (id 2060573, 10 views at 2060574–2060583) (2026-04-24)

### patterns/mapeditor

- [mapeditor-pattern.md](./tasks/completed/mapeditor-pattern.md) - Converted MapEditor from datamanagerclient into standalone DMS pattern (symbology CRUD via DMS instead of DAMA) (2026-03-17)
- [mapeditor-uda-migration.md](./tasks/completed/mapeditor-uda-migration.md) - MapEditor DAMA→UDA migration + symbology data port: fixed `mapeditor/siteConfig.jsx` type construction to produce `{pattern}|symbology` child types via `initializePatternFormat`; finished UDA path migration in page-pattern `map/` + `map_dama/` (source metadata, view data, hover, colorDomain); added `"Map: Dama Map"` registry alias; ported 247 DAMA symbologies into DMS with ID-map rewrite of 2,217 map components in mitigat-ny-prod (2 dangling stripped); new server-side `uda[pgEnv].viewsById[viewId].colorDomain[opts]` route with ckmeans/equalInterval/quantile/standardDeviation (PG-native, histogram-approximated ckmeans for 7M+ row sources); `filterToUda` state→UDA filter translator; idempotent migration verified; fixes uncovered during verification: DAMA paths in FilterControls/SymbologyControlMenu/LegendPanel/MapViewerLegend/PopoverControls/SourceLayout swapped to UDA, category-data group-by effect moved to UDA, `dataById` PK-column resolver (gis_datasets uses `ogc_fid` not `id`), category paint guard against empty data, legend row border transparent, layer panel overflow scrolling, `FilterableSearch` → new `ComboBox` UI component (drops `react-bootstrap-typeahead`), MapEditor list fetch attributes filtered to `name` (5MB → 10KB) (2026-04-18)

### patterns/forms

### patterns/admin

- [admin-site-edit-auth.md](./tasks/completed/admin-site-edit-auth.md) - Admin site-edit auth + separate create route: `${adminPath}/create` is public (no login, works on fresh deployment); `${adminPath}` protected with in-component guard (no site → create, not authed → login, no access → home); `isUserAuthed` in `admin/utils.js` checks auth pattern's `authPermissions` so admins can grant list-page access via `/list/manage_pattern/:auth_id/permissions`; `wrapper.jsx` null-guard prevents false redirect when loader hasn't completed; `dmsSiteFactory` shows Loading… instead of 404 on first load with no cache; auto-login after site creation (2026-05-21)
- [pattern-html-title.md](./tasks/completed/pattern-html-title.md) - Pattern-configurable HTML page title: added `html_title` text attribute to the `pattern` admin format, HTML Title input in the Overview pane of `PatternSettingsEditor`, and a `PatternTitle` SPA helper (`render/spa/utils/PatternTitle.jsx`) wrapped around each pattern's route in `pattern2routes` to set/restore `document.title` (resolved as `html_title.trim() || name || ''`). SSR title injection deferred (2026-05-07)
- [update-admin-theme-merges.md](./tasks/completed/update-admin-theme-merges.md) - Updated admin theme merges in siteConfig.jsx, editTheme.jsx, and themeEditor.jsx to use `mergeTheme` (2026-01-28)
- [admin-pattern-delete-duplicate.md](./tasks/completed/admin-pattern-delete-duplicate.md) - Added delete and duplicate buttons to admin pattern overview: ported actions from old PatternEdit modal to pattern editor Overview tab and list table (2026-02-08)

### patterns/auth

- [login-page-translate.md](./tasks/completed/login-page-translate.md) - Translated the TransportNY login page (`npmrds_auth` pattern 1498261, `selectedTheme: transportnyv2`) to `dms_design_system_v2/pages/login.html`. **Phase A (theme-only):** retired the dead `auth.login`/`auth.signup` draft block; styled via `auth.authPages.sectionGroup.default.*` (the surface `authLogin.jsx` actually reads — NOT `auth.login.*`); made the `layoutGroup` `auth` style borderless+centered (was a white card double-carding with `pageWrapper`); added `field.fieldWrapper` (kills the default `<fieldset>` border). **Phase B (component, key-gated/BC, user-approved):** extended `authLogin.jsx` with theme-key-gated slots — brand line, kicker+headline+subtitle (else falls back to plain `pageTitle`), "or" divider + SSO button, trailing `utilityLinks` row; each renders only when its key is set so every other site is byte-identical. SSO/divider left **commented** in transportny (no NY.gov provider; click shows a "not available" notice). **Phase C (FieldSet affordance):** moved the forgot link inline onto the password label row (tab order) via a new BC `labelAccessory` prop on `UI.FieldSet` + `field.labelRow`. Verified live via `card-shot` vs mockup. **Gotcha:** form rendered the auth base defaults until the pattern's `selectedTheme` pointed at `transportnyv2` (`src/themes/index.js` registers both `transportny` and `transportnyv2`). New skill `implementing-an-auth-login-page.md` written + indexed; auth gotcha folded into `translating-design-system-to-dms-theme.md` (2026-06-03)
- [auth-groups-byproject-shape.md](./tasks/completed/auth-groups-byproject-shape.md) - Fixed /groups/byproject response shape: wrapped plain array in { groups: [...] } with synthetic "public" group, 104 auth tests pass (2026-02-13)
- [view-as.md](./tasks/completed/view-as.md) - View As: admins (and groups with `'view-as'` permission) can impersonate any project user client-side — all `isUserAuthed` checks switch to the viewAs user's groups, mutations blocked via `globalThis.__dmsViewAsActive`, fixed amber `ViewAsBar` banner with exit, `public` group always injected into viewAs object. `isUserAuthed` promoted from `patterns/page/auth.js` (deleted) to canonical `utils/auth.js` to eliminate cross-pattern import. Works in single-tenant + multi-tenant. (2026-06-09)
- 2026-06-13 — `stat_value` KPI column type (figure + inline unit) — addendum on [map21-kpi-column-types.md](./tasks/completed/map21-kpi-column-types.md)
