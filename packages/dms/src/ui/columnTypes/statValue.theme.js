// Theme for the stat_value column type (ui/columnTypes/statValue.jsx).
// Sites override via theme.statValue; the unit's look can also be driven
// per-column with unitFontStyle (a textSettings token name).
export const statValueTheme = {
    wrapper: "inline-flex items-baseline gap-1.5",
    figure: "text-[40px] font-semibold leading-none tabular-nums",
    unit: "text-[0.4em] font-medium text-slate-500",
};
